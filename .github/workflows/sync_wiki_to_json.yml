name: Sync Wiki to Glossary JSON

on:
  gollum

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Main Repo
        uses: actions/checkout@v4
        with:
          path: main_repo

      - name: Checkout Wiki Repo
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}.wiki
          path: wiki_repo

      - name: Parse Wiki to JSON
        shell: python
        run: |
          import re
          import json
          import os

          wiki_path = 'wiki_repo/在线术语库.md'
          output_path = 'main_repo/glossaries/glossary-index.json'

          if not os.path.exists(wiki_path):
              print(f"Error: {wiki_path} not found.")
              exit(0)

          def is_chinese(s):
              return any('\u4e00' <= char <= '\u9fff' for char in s)

          def process_name(raw_name):
              # 1. 移除结尾的 "术语表" (支持空格)
              raw_name = re.sub(r'\s*术语表\s*$', '', raw_name.strip())
              
              # 2. 初步分割：处理显式分隔符 | 丨 /
              initial_parts = [p.strip() for p in re.split(r'[|丨/]', raw_name) if p.strip()]
              
              all_fragments = []
              for part in initial_parts:
                  # 3. 深度分割：处理混合在一起的 中文和非中文 (如 "Naruto 火影忍者")
                  # 匹配连续的中文字符 或 连续的非中文字符
                  fragments = re.findall(r'[\u4e00-\u9fff]+|[^\u4e00-\u9fff]+', part)
                  all_fragments.extend([f.strip() for f in fragments if f.strip()])

              if not all_fragments:
                  return raw_name, []

              # 4. 提取中文块作为主名称
              cn_fragments = [f for f in all_fragments if is_chinese(f)]
              
              if cn_fragments:
                  main_name = cn_fragments[0] # 取第一个中文块
              else:
                  main_name = all_fragments[0] # 没中文就取第一个块

              # 5. 其余所有块放入 tags (去重并保持顺序)
              tags = []
              for f in all_fragments:
                  if f != main_name and f not in tags:
                      tags.append(f)
                      
              return main_name, tags

          with open(wiki_path, 'r', encoding='utf-8') as f:
              content = f.read()

          sections = re.split(r'\n(?=### )', content)
          results = []

          for section in sections:
              if not section.strip().startswith('###'):
                  continue
              
              name_match = re.search(r'###\s+(.*)', section)
              url_match = re.search(r'\*\s+链接：(?:\[.*?\]\()?(https?://[^\s\)]+)\)?', section)
              maint_match = re.search(r'\*\s+维护者：\[(.*?)\]\((.*?)\)', section)

              if name_match and url_match and maint_match:
                  raw_name = name_match.group(1).strip()
                  main_name, tags = process_name(raw_name)
                  
                  url = url_match.group(1).strip()
                  maintainer = maint_match.group(1).strip()
                  feedback = maint_match.group(2).strip()

                  if feedback.startswith('mailto:'):
                      feedback = feedback.replace('mailto:', '')

                  results.append({
                      "name": main_name,
                      "tags": tags,
                      "url": url,
                      "maintainer": maintainer,
                      "visibility": True,
                      "feedback": feedback
                  })

          os.makedirs(os.path.dirname(output_path), exist_ok=True)
          with open(output_path, 'w', encoding='utf-8') as f:
              json.dump(results, f, ensure_ascii=False, indent=2)
          
          print(f"Processed {len(results)} items.")

      - name: Commit and Push
        run: |
          cd main_repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add glossaries/glossary-index.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "docs: auto-update glossary-index.json (cleaned names & tags)"
            git push origin main
          fi
